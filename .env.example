# Скопируйте в .env.local и заполните.

OPENAI_API_KEY=sk-...

# Модель для реплик NPC (например gpt-4o-mini, gpt-5.1-nano — как доступно в вашем аккаунте).
NPC_MODEL=gpt-4o-mini

# Опционально: отдельная модель для summarize_dialogue в /api/npc/[npcId]/event.
# NPC_SUMMARY_MODEL=gpt-4o-mini
